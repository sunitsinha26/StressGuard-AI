// LiveMonitor.jsx - re-exported from App.jsx's LivePage for compatibility
// The full LiveMonitor is now built into App.jsx as LivePage
export default function LiveMonitor({ token }) {
  return null; // Handled by LivePage in App.jsx
}
